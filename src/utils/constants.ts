const SEARCH_PARAMS = {
  redirectTo: 'redirectTo',
  error: 'error',
  token: 'token',
}

const otpLength = 6
const bcryptSalt = 10

const PLACEHOLDER = {
  email: 'john@example.com',
  password: '********',
}

export { bcryptSalt, otpLength, PLACEHOLDER, SEARCH_PARAMS }
